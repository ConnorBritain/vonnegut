## Findings

None.

## Immaterial losses

- **"Edward the Fourth"** — not a fidelity loss; see *Scanner defects*.

## Scanner defects

- The scan reports `"Edward the Fourth"` as a named entity absent from the revision. The revision carries the entity across a line break at revision lines 8–9 (`"Edward the"` / `"Fourth of England"`). The matcher appears to compare atoms against single lines, or against text in which the newline was not normalised to a space, so any entity spanning a wrap is reported absent. This is a false positive of the presence check, not a consolidation by the reviser.

## Notes on non-scan classes

- **claim-drift**: none. Every hedge and scope marker in the original survives — `not always most seen` (orig. 3–4 → rev. 3–4), `But this holds not always` (orig. 7 → rev. 7), `for the most part` (orig. 29 → rev. 30), `many times` (orig. 25 → rev. 26), `not but I think` (orig. 20 → rev. 20). Attributions (`“Pulchrorum autumnus pulcher;”`) and the full six-name list are intact.
- **dropped-qualification**: none found.
- **structural**: the revision inserts two paragraph breaks (rev. 11, 23) with no headings in either file and no content dropped at the seams.
- **outside-plan**: no edit plan was supplied with this case, so this class is not assessable.

**FAITHFUL**
