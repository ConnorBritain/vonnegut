# v0.2 profile render failure — support syntax and zero-absence status

This is the preserved replacement six-render set against implementation `95f748e`. It is
not acceptance evidence and its lock is deliberately named `failed-corpus.lock.json`.
The run used Sonnet at low effort with concurrency one. No output from the preceding
failed set was reused.

All six calls completed and emitted parseable `voice-profile/2` envelopes. Doctorow r3
discussed first-person personal experience and personal testimony, but the paragraph
coverage analyzer's self-reference/biography vocabulary recognized only narrower terms.
The following implementation adds these ordinary phrasings and fixtures the exact failed
language.

The run also contains genuine renderer failures. EFF Mullin r1 used phrases such as
`all 11 samples` and `established across all 11 samples` instead of placing the required
literal `11/11` support token in four observation paragraphs. EFF Mullin r2 emitted the
zero-count profanity observation as a `rated` dimension instead of the absence side of an
`absent-paired` dimension. The validator rejected both. The following implementation adds
a numbered final audit for literal support tokens, rate locators and arithmetic, zero-rate
pairing, prose coverage, and derived status.

No profile, draft, refusal, or critic draw from this directory is eligible for reuse. A
new implementation commit requires a complete new six-render stability set and, if that
passes, a complete new 20-draft acceptance run.
