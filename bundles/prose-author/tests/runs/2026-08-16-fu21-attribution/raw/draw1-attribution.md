# FU-21 draw 1 — the named-source observation, verbatim

> **The floor is handed to a named ally with an attributive tag, and the argument then
> builds on them — 30 tags, 7/10 samples, several times per piece** (rate 1.71/1,000):
> *"As Weil writes"*, *"as Rushkoff calls it"*, *"As Quiggin writes"*, *"John Gruber's
> term for"*.

Present, and RATED — this render found the habit countable (30 attributive tags) where
draw 3 recorded it unrated. Both are defensible; the prompt requires a rate only where
instances can be enumerated, and an attributive tag can be.

## Incidental: the renderer rejected FU-14's original hypothesis

Draw 1's section 8 lists observations it formed and dropped for lack of support. Among
them:

> that paragraphs end on their shortest sentence

That is precisely the belief three failed FU-14 fixes were built on. The corpus ends
paragraphs LONG (median 28 words against a 23-word overall median), and a renderer working
the corpus directly declined to record the opposite for want of evidence.
