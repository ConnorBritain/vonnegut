# v0.2 profile dispatch — invalid counting-rule locators

This is preserved structural-failure evidence, not acceptance evidence.

Doctorow render 1 completed under pinned commit
`6a88d920af733c3136ae57e5b188904d641b5d91`. Its schema, ten coverage rows,
counts, and rates were otherwise present, but Sonnet paraphrased every deterministic
`counting_rule` between the supplied measurements, profile prose, and JSON. The validator
therefore reported ten unlocatable rules. Render 2 was interrupted after the common
contract failure was established; no other response completed.

No output from this attempt is reused. The replacement contract assigns every analyzer
rule a stable `[measurement:<id>]` locator and requires that locator in prose and JSON.
